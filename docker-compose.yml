version: '3.8'
services:
  shieldtube-api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./cache:/app/cache
    environment:
      - CACHE_DIR=/app/cache
      - FFMPEG_THREADS=${FFMPEG_THREADS:-2}
    restart: unless-stopped
